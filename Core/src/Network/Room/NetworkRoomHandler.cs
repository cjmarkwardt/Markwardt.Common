namespace Markwardt;

/*public interface INetworkRoomHandler<TMessage>
{
    void OnStarted(INetworkRoom<TMessage> room);
    void OnJoining(INetworkRoom<TMessage> room);
    void OnJoined(INetworkRoom<TMessage> room);
    void OnPortOpened(INetworkRoom<TMessage> room, INetworkPort port);
    void OnPortClosed(INetworkRoom<TMessage> room, INetworkPort port, Exception? exception);
    void OnMemberJoined(INetworkRoom<TMessage> room, string name);
    void OnReceived(INetworkRoom<TMessage> room, string sender, bool isBroadcast, TMessage message);
    bool OnRouted(INetworkRoom<TMessage> room, string sender, string? target, TMessage message);
    void OnMemberLeft(INetworkRoom<TMessage> room, string name);
    void OnLeft(INetworkRoom<TMessage> room, Exception? exception);
}*/