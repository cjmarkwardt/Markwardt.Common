namespace Markwardt;

/*public class BufferPool<T>(int initialCapacity = 256, int maxCapacity = 1024, int? retention = null)
    : Pool<IBuffer<T>>(() => new Buffer<T>(initialCapacity), x => { x.Length = 0; x.Clamp(maxCapacity); }, retention);*/